# Brain

Append durable decisions and preferences here. One pipe line per concept. Reuse the same `id` when updating. Do not put `|` inside a field. Skip ephemeral tasks, duplicates, and secrets.

```text
t|id|s|w|r
```

`d` = decision · `p` = preference

```text
d|runtime-node|Ship on Node 22 LTS|align with hosting|-
p|avoid-redux|No Redux unless concurrency proves it|-|frontend/
```

Append after architecture or stack choices, repeated human preferences, or post-review decisions worth keeping.
